apply plugin: 'java'
archivesBaseName = 'NBAOracle'
version = '1.0'
apply plugin: 'application'
mainClassName = 'Scraper'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.jsoup:jsoup:1.7.2'
}

jar {
    manifest {
        // fix this
        // attributes('Main-Class': 'Scraper', 'Class-Path': configurations.compile.collect { it.getName() }.join(' '))
    }
}
